# All of these are PUBLIC values (they ship to the browser). Security is enforced
# by Supabase Row Level Security, not by keeping these secret.
# Defaults live in src/config.js; set these only to point at a different project.
VITE_SUPABASE_URL=https://xxxxxxxxxxxxx.supabase.co
VITE_SUPABASE_ANON_KEY=eyJ...
VITE_TURNSTILE_SITE_KEY=0x4AAAA...
